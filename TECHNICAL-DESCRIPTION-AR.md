# الوصف الفني المختصر

## الأدوات المستخدمة

تم بناء النموذج باستخدام React داخل بيئة Vite، مع React Router لإدارة الصفحات، وCSS مخصص لدعم الشاشات المختلفة واتجاه RTL، ومكتبة Phosphor للأيقونات. تستخدم الجولة Matterport Embed SDK، وتحفظ التقييمات وبيانات المعايرة التطويرية داخل LocalStorage فقط.

## هيكل الموقع

يعتمد الموقع على مكوّن Layout موحد للتنقل والتذييل، وتسع صفحات بحثية. تفصل الجولة بين سجلات القطع وسجلات النقاط ثلاثية الأبعاد، وتربطهما بالمعرّفات مع أداة تحقق تمنع عرض السجلات غير المعتمدة للزوار.

## فكرة الجولة التخيلية

تعرض الجولة نموذج Matterport الحقيقي `SFcW4AeVYWM`. تُلتقط النقطة على سطح القطعة بإحداثيات Matterport ثلاثية الأبعاد، مع حفظ الدور وموضع المسح وحالة المراجعة. لا توجد قطع معتمدة حاليًا؛ يجب تحديد كل قطعة ومراجعة بياناتها وصورتها يدويًا قبل ظهورها للزوار.

## فكرة أمن المعلومات

تشرح الصفحة — بصريًا — مستويات المستخدمين، ومنع التعديل إلا للمسؤول، وحماية الملفات، والنسخ الاحتياطي، وسجل التغييرات. هذه مفاهيم مقترحة وليست نظام حماية backend فعليًا.

## طريقة التشغيل

بعد تثبيت Node.js وpnpm، تُنفذ `pnpm install --frozen-lockfile` ثم `pnpm run dev`. تُنفذ الاختبارات بواسطة `pnpm test` والتحقق من النقاط بواسطة `pnpm run validate:hotspots`. ولإنشاء نسخة الإنتاج تُنفذ `pnpm run build`، وتظهر الملفات النهائية داخل مجلد `dist`.
